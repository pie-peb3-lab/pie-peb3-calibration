name: PEB-3E Controlled PR State Calibration V1 Retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse superseded execution path
        run: |
          echo 'PEB-3E V1 is retired. No dispatch performed.'
          exit 0
